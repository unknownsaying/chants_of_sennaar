#include "math.h"
#include "string.h"
#include "strings.h"
#include "stringlist.h"
void equation(){
    // TODO: implement this
    // equation is a string containing the equation to be evaluated
    // x, y, z, t are pointers to the values of the variables in the equation
    // the function should evaluate the equation and store the result in *x, *y, *z, *t
    // for example, if equation is "x + y + z + t", then *x = x, *y = y, *z = z, *t = t
    // if equation is "x + y", then *x = x + y, *y = x + y, *z = 0, *t = 0e
    char radius, t;
    radius = 'X*X + Y*Y + Z*Z';
    t = sqrt(radius);
}
